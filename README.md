# cycleTimeCalculator

## テスト

コミット前に以下を実行してください。

```bash
npm run test:all
```

実行内容:

- `npm run check:syntax` : `workspace/index.html` の `<script>` 構文チェック
- `npm test` : `simulationLogic` / `renderLogic` の単体テスト
